<%- partial('components/header/progress-bar') %>
<% if (theme.global.preloader === true || theme.global.preloader.enable === true) { %>
<%- partial('components/header/preloader') %>
<% } %>
<main class="page-container" id="swup">

	<% if (theme.home_banner.enable === true && is_home() && !page.prev) { %>
	<%- partial('pages/home/home-banner') %>
	<% } else if (theme.home_banner.enable === true && is_home() && page.prev && theme.home_banner.style === "fixed") { %>
	<%- partial('pages/home/home-background') %>
	<% } %>

	<div class="main-content-container flex flex-col justify-between min-h-dvh">
		<div class="main-content-header">
			<%- partial('components/header/navbar') %>
		</div>

		<%# The bento grid absorbs the sidebar: its cards are rendered as grid tiles
			by pages/home/home-content, so the rail is only built for the classic
			single-column list. Two layout systems side by side is what the grid
			replaced. %>
		<% const homeRail = is_home() && theme.home.bento === false && theme.home.sidebar.enable === true; %>
		<div class="main-content-body transition-fade-up">
			<% if (homeRail && theme.home.sidebar.position === "left") { %>
			<%- partial('pages/home/home-sidebar') %>
			<% } %>

			<div class="main-content<%= is_home() ? ' is-home' : '' %>">
				<%- partial(getPagePartialPath(page)) %>
			</div>

			<% if (homeRail && theme.home.sidebar.position === "right") { %>
			<%- partial('pages/home/home-sidebar') %>
			<% } %>
		</div>

		<div class="main-content-footer">
			<%- partial('components/footer/footer') %>
		</div>
	</div>

	<%# The vault gate is a `page`, not a post — is_post() is false for it — but
		what it mounts IS an article, table of contents and all. Without the tools
		rail the TOC toggle does not exist, and the controller that reaches for it
		on every article throws before the table of contents is ever measured. %>
	<% if (is_post() || page.type === "vault") { %>
	<div class="post-tools">
		<%- partial('pages/post/post-tools') %>
	</div>
	<% } %>

	<div class="right-side-tools-container">
		<%- partial('utils/side-tools') %>
	</div>

	<%- partial('utils/image-viewer') %>

	<% if (theme.navbar.search.enable) { %>
	<%- partial('utils/local-search') %>
	<% } %>

</main>

<% if (theme.global.single_page !== false) { %>
<%- partial('components/swup') %>
<% } %>