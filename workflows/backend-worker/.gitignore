node_modules/
.wrangler/
.dryrun/
# Local/secret config — commit the *.example templates instead.
.dev.vars
wrangler.toml

# Local D1 dumps from `npm run db:backup`. They contain reader data.
d1-backup*.sql
